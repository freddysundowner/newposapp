import 'package:flutter/cupertino.dart';

class CashflowGroups extends StatelessWidget {
  const CashflowGroups({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
